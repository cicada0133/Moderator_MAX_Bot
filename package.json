{
  "name": "max-chat-moderator-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MAX chat moderation bot that removes messages with Russian profanity.",
  "scripts": {
    "check": "node scripts/run-with-ca.js src/check-token.js",
    "start": "node scripts/run-with-ca.js src/polling.js",
    "start:webhook": "node scripts/run-with-ca.js src/webhook-server.js",
    "webhook:subscribe": "node scripts/run-with-ca.js src/subscribe-webhook.js",
    "dictionary:show": "node scripts/show-dictionary.js",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "russian-bad-words": "^0.5.0"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
